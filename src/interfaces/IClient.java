package interfaces;

public interface IClient {

    /**
     * Starts the client connection, initializes communication with
     * the server, and launches the interactive menus.
     */
    void start();
}